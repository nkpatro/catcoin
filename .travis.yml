 install:
   cd src
   make -f makefile.unix
 language: cpp
 before_install:
     apt-get install -qq libssl-dev
     apt-get install -qq libdb4.8-dev
     apt-get install -qq libboost-dev
     apt-get install -qq miniupnpc-dev

branches:
  only:
    - master
    - stable
